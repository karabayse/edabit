

/*
showdown(
  "   Bang!        ",
  "        Bang!   "
) ➞ "p1"

 // p1 draws his gun sooner than p2

showdown(
  "               Bang! ",
  "             Bang!   "
) ➞ "p2"

showdown(
  "     Bang!   ",
  "     Bang!   "
) ➞ "tie"
*/

function showdown(p1, p2) {
  let arr1 = p1.split('');
  let arr2 = p2.split('');

  // loop through array and determine where in the array the first value exists
  for (var i = 0; i < arr1.length; i++) {
    arr1[i]
  }

  // compare where first value in arr1 and arr2 exists 
}
